<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="../style/style.css">
</head>
<body>

<form action="/update/<%= post.id %>" method="POST" enctype="multipart/form-data" class="edit">
    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" class="form-control" id="title" name="title" value="<%= post.title %>">
    </div>
    <div class="form-group">
        <label for="content">Content</label>
        <textarea class="form-control" id="content" name="content" rows="6"><%= post.content ? post.content : '' %></textarea>
    </div>
    <div class="form-group">
        <label for="image">Image</label>
        <input type="file" class="form-control-file" id="image" name="image">
        <% if (post.image) { %>
            <img src="/uploads/<%= post.image %>" alt="<%= post.title %>" style="max-width: 200px; margin-top: 10px;">
        <% } %>
    </div>
    <button type="submit" class="btn btn-primary">Update</button>
</form>

</body>
</html>
